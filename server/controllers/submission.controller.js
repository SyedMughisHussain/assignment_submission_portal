import Submission from "../models/submission.model.js"

const makeSubmission = (req, res) => {
    try {
        const { assignmentId, githubUrl, comment } = req.body;



    } catch (error) {
        
    }
}

export { Submission }